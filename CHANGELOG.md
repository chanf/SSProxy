# CHANGELOG

`luci-app-ssproxy`（水杉代理）所有重要变更记录于此，按时间倒序排列。

版本号遵循 `1.0.0-N`，每次构建由 `build_ipk.py` 自动递增（详见 `CLAUDE.md`）。本项目无正式 release tag，下方以「日期 + 当日触及的版本区间」作为分节。

分类图例：🆕 Added　🔧 Changed　🐛 Fixed　📚 Docs　🧪 Tests　⚙️ Internal

---

## 未发布（Unreleased / 工作区未提交）

- 🆕 **「受控 IP 列表」与「劫持系统 DNS」共存**（IPv4 + IPv6）：不再在 `dns_hijack=1` 时把白名单静默强转为 `all`。新增 nft 表 `inet mihomo_dns`（nat prerouting）按源 IP 把白名单设备的 53 端口 `dnat` 到 Mihomo DNS，其余设备保留 dnsmasq 真实上游直连。
- 🆕 `helper.sh` 新增纯函数 `emit_tproxy_rules`（输出 nft 规则文本，`nft -f -` 应用）、`get_lan_ip` / `get_lan_ip6`（路由器 LAN 地址探测）；`init.d` 补 IPv6 策略路由与停机清理。
- 🧪 新增 `tests/shell/test_tproxy_rules.py`（14 例）+ `ip` 测试桩；`test_prepare_config.py` 断言 `ipv6: true`。
- 📚 新增 `docs/whitelist-dns-coexistence-design.md`，更新 `docs/whitelist-test-cases.md`（TC-06 共存用例）。

---

## 2026-07-19（1.0.0-149 ~ 1.0.0-159）

- 🆕 规则管理页增加「自定义规则 vs 订阅规则」说明提示框。*(c0949ce, -159)*
- 🆕 **配置模式切换**（订阅 / 仅自定义 / 混合）+ LAN IP 识别用于 ACL；🐛 修复混合模式下 `rules:` 追加位置错误。*(b474457, -158)*
- ⚙️ 打包器自动生成 `dist/releaseNote.md`（从 git 提交提取）+ 通用闭环开发经验文档 `docs/LuCI插件自动化闭环开发流程.md`。*(7012759, -151)*
- 🆕 流量统计新增「按天汇总」「按月汇总」永久存储列表（北京时间）。*(7f282aa, -150)*
- 🆕 流量统计独立页 + 连通性测试 / 自动选节点 / 下载日志 + 多项健壮性修复。*(8c024a5)*

## 2026-07-18（1.0.0-134 ~ 1.0.0-135）

- 🆕 **商业化加固**：节点选择持久化 + 控制器密钥（Secret 自动生成）+ Geo 数据库管理（GeoIP/GeoSite 镜像与自动更新）。*(b191d97, -135)*
- 🧪 新增构建器与 `helper.sh` 单元测试套件（104 例）；🐛 修复 JSON 引号转义与构建可复现性。*(ebc8690, -134)*

## 2026-07-14（1.0.0-121 ~ 1.0.0-125）

- 🆕 规则管理新增「清空所有规则」按钮。*(175f0a9, -125)*
- 🔧 规则管理打开页面不再加载 UCI 规则，改为按需加载以加速。*(1f5b222, -123)*
- 🆕 规则管理 UCI 列表默认收起 + 覆盖 / 追加双导入模式。*(f57d149, -121)*

## 2026-07-13（1.0.0-101 ~ 1.0.0-117）

- 🆕 启停操作防重（系统级操作状态锁）。*(0211261, -117)*
- 🆕 规则管理批量导入规则 + 规则说明文档与开发总结。*(340fd04, -109)*
- 🆕 仪表盘页面显示插件版本号。*(3d1f143, -102)*
- 📚 文档对齐代码：修正分流决策树与「红杏」判定的措辞偏差。*(a0d1bd9, -101)*
- 🔧 重新设计访问日志并重构 IP 白名单分流决策与清理机制。*(7c38c0b)*

## 2026-07-12（初始版本 ~ 1.0.0-100）

- 📚 补充完善产品设计文档，新增主项目 `README.md`。*(04331ad)*
- 🆕 **客户端 IP 白名单转发** + 划分独立「规则管理」Tab；🐛 修复日志展现 Bug。*(eb44740)*
- 🐛 节点延时测试修复 + 仪表盘 null 修复 + 代码格式规范化。*(c19ccda)*
- 🆕「系统代理日志」面板新增「清空」按钮。*(752c38e)*
- 🔧 升级 Mihomo 核心至 **v1.19.28**（支持 anytls 等新型代理）。*(0bd0bb3)*
- 🐛 统一「运行状态」与各面板的服务状态判断，消除 RUNNING 误报。*(af52a85)*
- 🐛 修复核心在 J4125 等 非 v3 CPU 上启动崩溃（控制器 9090 无监听）。*(a3bb68f)*
- 🔧 节点列表在核心未运行时不再误导显示「测试」。*(a3df133)*
- 🐛 修复「测试」节点延时全部显示失败的问题。*(a5dc0fa)*
- 🔧 将 `/etc/config/mihomo` 标记为 conffile，避免升级后订阅链接丢失。*(0d2e7ed)*
- ⚙️ 构建触发 `PKG_VERSION` 自动递增（-41 → -42）。*(9d28784)*
- 🔧 移除「服务设置」中的「启用服务」勾选框。*(eef2299)*
- 🆕 节点列表改为自适应小卡片并新增「测试」延时按钮。*(f6862ac)*
- 🆕 新增**「访问日志」页面**：实时连接、历史采集与按域名规则管理。*(86fc2d3)*
- 🆕 缓存订阅链接 + 访问日志设计文档。*(f4fd030)*
- ⚙️ 构建后版本号自动递增（1.0.0-26 → 1.0.0-27）。*(d0d7c51)*
- 🐛 修复订阅更新后节点列表为空并增加操作日志（**首个提交**）。*(cdd3fee)*
